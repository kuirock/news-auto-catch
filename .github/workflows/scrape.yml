name: News Scraper

on:
  schedule:
    - cron: '0 0-10,22-23 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: コードを取得
        uses: actions/checkout@v3

      - name: Pythonをセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: ライブラリをインストール
        run: |
          pip install requests beautifulsoup4 supabase

      - name: スクレイピング実行
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          PORTAL_COOKIE: ${{ secrets.PORTAL_COOKIE }} # 👈 Cookieを使う設定に戻す
        run: python main.py